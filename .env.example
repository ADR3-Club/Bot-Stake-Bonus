# ========================================
# DISCORD CONFIGURATION
# ========================================

# Discord Bot Token (get it from https://discord.com/developers/applications)
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN

# Discord Channel ID where to post the bonus codes
# Right-click on channel → Copy ID (enable Developer Mode in Discord settings)
CHANNEL_ID=YOUR_CHANNEL_ID

# Discord Role ID to ping (optional)
# Right-click on role → Copy ID
PING_ROLE_ID=

# ========================================
# TELEGRAM CONFIGURATION
# ========================================

# Telegram API credentials (get them from https://my.telegram.org)
# 1. Go to https://my.telegram.org
# 2. Login with your phone number
# 3. Click "API development tools"
# 4. Create a new application
# 5. Copy the API ID and API Hash
TG_API_ID=YOUR_TELEGRAM_API_ID
TG_API_HASH=YOUR_TELEGRAM_API_HASH

# Telegram channels to monitor (comma-separated, can be usernames or IDs)
# Supports FOUR types of channels:
#
#   1. SPOILERS: Code masqué dans un spoiler (ex: @StakecomDailyDrops)
#      → Le bot détecte automatiquement les codes dans les spoilers
#
#   2. CACHE RAINSTEAM: Messages séparés (ex: @RainsTEAM)
#      → Message 1: "FINAL BONUS DROP INCOMING!" avec conditions
#      → Message 2: code seul (ex: "bestchat")
#      → Le bot met en cache les conditions et publie quand le code arrive
#
#   3. VIP NOTICES: URLs playstake.club (Weekly, Monthly, etc.)
#      → https://playstake.club/bonus?code=XXXX
#      → Détection automatique du type de bonus
#
#   4. OCR: Images ou vidéos avec code visible
#      → Le bot extrait automatiquement le code depuis l'image/vidéo
#      → Preprocessing optimisé pour meilleure détection
#
# Examples:
#   - Username: @StakecomDailyDrops
#   - Channel ID: -1001234567890
#   - Multiple: @StakecomDailyDrops,@RainsTEAM,-1001234567890
#
# To find channel IDs, use: npm run list-channels
TG_CHANNELS=

# Telegram string session (will be generated on first run)
# On first start, the bot will ask for:
#   - Your phone number
#   - The verification code (SMS/Telegram)
#   - Your 2FA password (if enabled)
# After successful login, copy the generated session here to avoid re-authentication
TG_STRING_SESSION=

# ========================================
# CUSTOMIZATION (Optional)
# ========================================

# Button label text (default: 🎁 Lien du code)
# Maximum 80 characters
BUTTON_LABEL_TEXT=🎁 Lien du code

# Image URL for the Discord embed (default: provided)
# Leave empty to disable image
BONUS_IMAGE_URL=https://cdn.discordapp.com/attachments/1290178652327252009/1413168626780995704/Logo_Bonus_v2.png

# ========================================
# DEBUG & MONITORING (Optional)
# ========================================

# Enable debug mode for Telegram (0 or 1)
# When enabled, shows detailed logs:
#   - Message text
#   - Entities detected
#   - Spoilers found
#   - OCR results
#   - Cache operations
DEBUG_TELEGRAM=0

# Send a health ping message on startup (0 or 1)
# Useful to confirm the bot is running and connected
TG_HEALTH_PING=0
